return {
  configfile = ".luacov";

  statsfile = "luacov.stats.out";

  reportfile = "luacov.report.json";

  runreport = false;

  deletestats = false;

  include = {
    "/pop3$",
    "/pop3/.+$",
  };

  exclude = {
  };

  coveralls = {
    pathcorrect = {
      {"^.-[/\\]share[/\\]lua[/\\]5.%d", "lua"};
    }
  };

}
